- run frontend with 'npm run dev'
- Added a new file api.js to help with backend integration
- The chatgpt reponses don't seem to be working because of an invalid API key but the other models are working fine

